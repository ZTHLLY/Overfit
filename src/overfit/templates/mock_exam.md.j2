{# The question paper. Answers live in a separate file on purpose, so the
   student can attempt the paper before seeing them -- testing yourself and
   then checking is a great deal more useful than reading questions with the
   answers underneath. #}
# {{ course }} — Practice Exam

*{{ items | length }} questions generated from {{ sources | length }} source
document{{ 's' if sources | length != 1 }} on {{ generated_at }}.*

{% for topic, group in grouped %}
## {{ topic }}

{% for item in group %}
**{{ item.number }}.** {{ item.question }}

{% endfor %}
{% endfor %}
---

Answers: `{{ answers_file }}`

<sub>Generated by Overfit from your own course material. Every answer cites the
file and page it came from — check anything that looks wrong against the
source.</sub>
